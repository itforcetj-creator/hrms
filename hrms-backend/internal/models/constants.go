package models

// Working days per month used in payroll calculations.
const WorkingDaysPerMonth = 22.0

// Default daily leave accrual rate.
const DailyLeaveAccrualRate = 1.5

// Default leave balance for new employees.
const DefaultLeaveBalance = 20.0

// Late arrival threshold — check-in after this time is considered late.
const LateArrivalThreshold = "09:15:00"

// Default password for newly hired candidates (should be changed on first login).
const DefaultCandidatePassword = "Welcome2026!"
